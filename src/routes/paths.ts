const paths = {
    LOGIN: "/authentication/sign_in",
    REGISTER: "/authentication/sign_up",
    FOGOTPASSWORD: "/authentication/fogot_password"
}

const routerPath = {
    AUTH: '/authentication',
    LOGIN: 'sign_in',
    REGISTER: "sign_up",
    FOGOTPASSWORD: "fogot_password"
}

export {paths, routerPath}

